# 前端环境变量配置示例
# 复制此文件为 .env 并填入实际值

# API基础URL（可选，默认为相对路径 /api/v1）
VITE_API_BASE_URL=http://localhost:8088

# 加密密钥配置（已弃用）
# 现在加密密钥由后端自动下发，无需在前端配置
# 如果后端启用了加密，前端会自动获取密钥并使用
# 注意：请确保后端 config.yml 中的 encryption.enabled = true
# VITE_ENCRYPTION_KEY=

# 应用标题（可选，会覆盖从后端获取的标题）
VITE_APP_TITLE=敏感词检测系统
